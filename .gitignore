.venv/
__pycache__/
*.pyc
.claude/
.DS_Store

# Built fresh by each scan and uploaded straight to Pages -- committing it
# would just be a daily 100 KB churn commit against generated data.
site/roles.json

# Same: master_resume.tex parsed into JSON, rewritten by every scan from a
# file that *is* committed, so nothing here is history.
site/master.json

# The Worker bundles @anthropic-ai/sdk; wrangler installs it at deploy time.
worker/node_modules/
